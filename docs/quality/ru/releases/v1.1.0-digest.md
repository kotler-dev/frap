# Frap Release Quality Digest — v1.1.0

> Backfill минорного релиза semantic locators (тег `java-v1.1.0`).
> Baseline: линия `java-v1.0.1`.

## 1. Краткое резюме

- Пайплайн CLASSIFY→SCOPE→LOCATE→VERIFY→HEAL в Core.
- Maven 1.1.0: core, playwright, mcp.
- PoG с `getByRole` / `getByTestId`, scoped locators.
- Пробелы (закрыты в 1.1.1): неполный SnapshotBuilder, extract в `/tmp`.

## 2–8. Объём, метрики, тесты

Контракты `contract_locator_quality` и `contract_dom_benchmark`; dom-benchmark 01–09; e2e + MCP verify. Performance — N/A.

## 9. До и после

Дельта к 1.0.1: семантическое ранжирование, MCP 1.1.0, ground-truth dom-benchmark.

## 10. Ограничения

Отставание Java snapshot от MCP `snapshot.js` (исправлено в 1.1.1).

## 11. Воспроизведение

```bash
cd frap/crates && cargo test -p frap-core --test contract_locator_quality --test contract_dom_benchmark
./scripts/run-java-e2e.sh
./scripts/run-frap-mcp-verify.sh
```

# Frap Release Quality Digest — v1.1.0

> Backfill for the semantic-locator minor release (tag `java-v1.1.0`).
> Baseline: `java-v1.0.1` line. Reproduce contracts only — no historical quality-report JSON.

## Report menu

| Category | Section |
|----------|---------|
| Executive summary | [§1](#1-executive-summary) |
| Scope | [§2](#2-scope--methodology) |
| Locator quality | [§3](#3-locator-quality) |
| Discover | [§4](#4-discover--clustering) |
| PoG | [§5](#5-page-object-generation) |
| Guarantees | [§6](#6-behavioral-guarantees) |
| Tests | [§7](#7-test-pyramid) |
| Performance | [§8](#8-performance) |
| Before/after | [§9](#9-before--after) |
| Caveats | [§10](#10-caveats--non-claims) |
| Reproduction | [§11](#11-reproduction) |

## 1. Executive summary

- **Minor:** CLASSIFY→SCOPE→LOCATE→VERIFY→HEAL semantic pipeline in Core.
- **Maven:** `frap-core-java`, `frap-playwright`, `frap-mcp-*` @ **1.1.0**.
- **PoG:** `getByTestId`, `getByRole`, scoped locators; `LocatorRecommendation.scope`, `filter_text`, `match_count`.
- **Gap (fixed in 1.1.1):** Java `SnapshotBuilder` without full collector fields; `/tmp` binary extract risk.

## 2. Scope & methodology

Shipped semantic ranking + dom-benchmark alignment (pages 01–09). MCP 6 tools @ 1.1.0 on Central.

## 3. Locator quality

`contract_locator_quality` — plan thresholds on 65-element fixture (introduced/locked with semantic pipeline).

## 4. Discover & clustering

`contract_dom_benchmark` — 9 pages, semantic `locator.value` ground truth.

## 5. Page Object generation

`generate_page_object` emits Playwright Java with semantic accessors when strategies allow.

## 6. Behavioral guarantees

`run-java-e2e.sh`, `run-frap-mcp-verify.sh`, dom-benchmark E2E.

## 7. Test pyramid

Same contract + e2e stack as 1.0.1 line; expanded locator-quality fixture tests.

## 8. Performance

N/A (not measured at 1.1.0 release).

## 9. Before / after

Δ vs **1.0.1:** semantic pipeline, MCP 1.1.0 coordinates, dom-benchmark semantic values.

## 10. Caveats & non-claims

SnapshotBuilder collector lag vs MCP `snapshot.js` (addressed in **1.1.1**).

## 11. Reproduction

```bash
cd frap/crates && cargo test -p frap-core --test contract_locator_quality --test contract_dom_benchmark
./scripts/run-java-e2e.sh
./scripts/run-frap-mcp-verify.sh
```

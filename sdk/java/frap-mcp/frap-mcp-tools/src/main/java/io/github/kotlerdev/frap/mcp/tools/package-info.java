/**
 * Transport-neutral frap MCP tools.
 *
 * <p>This package will host the {@code @McpTool} service ({@code FrapTools}),
 * the {@code FrapCoreClient} bean configuration ({@code FrapCoreConfig}) and the
 * {@code SnapshotScript} resource loader. The tools builder fills these in; this
 * package-info exists so the module compiles as a plain library jar in the
 * foundation phase.</p>
 */
package io.github.kotlerdev.frap.mcp.tools;

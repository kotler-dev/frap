# Streamable-HTTP transport: normal web server, normal logging (stdout is free here,
# unlike the stdio runner where stdout is reserved for the JSON-RPC stream).

# MCP server identity / transport.
# protocol=STREAMABLE selects the streamable-http transport provided by
# spring-ai-starter-mcp-server-webmvc (Spring MVC). The tools are the same
# transport-neutral @McpTool methods scanned from io.github.kotlerdev.frap.mcp.tools.
spring.ai.mcp.server.name=frap-mcp-http
spring.ai.mcp.server.version=1.0.1
spring.ai.mcp.server.type=SYNC
spring.ai.mcp.server.protocol=STREAMABLE
spring.ai.mcp.server.instructions=frap MCP server (streamable-http). Exposes 5 frap tools over the bundled native frap-core-rpc binary.

# I/O mode: http = inline-mode (content in JSON, as before). The HTTP client may
# be remote with no shared filesystem, so artefacts stay inline in the tool
# request/response (no file paths). Activates FrapTools via @ConditionalOnProperty.
frap.io.mode=inline

# Streamable-http endpoint. Single endpoint handling all JSON-RPC traffic
# (initialize / tools/list / tools/call) via HTTP POST; the same path streams
# server-to-client notifications. Default is /mcp; set explicitly for clarity
# and so the README can document a stable path.
spring.ai.mcp.server.streamable-http.mcp-endpoint=/mcp
spring.ai.mcp.server.streamable-http.keep-alive-interval=30s

server.port=8080
spring.main.banner-mode=off
